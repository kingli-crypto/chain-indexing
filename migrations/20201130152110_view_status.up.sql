CREATE TABLE view_status
(
    key character(128) COLLATE pg_catalog."default" NOT NULL,
    value character varying COLLATE pg_catalog."default",
    CONSTRAINT view_status_pkey PRIMARY KEY (key)
)

